import { StyleSheet } from 'react-native';

const EPISODE_STILL = StyleSheet.create({
  still: {
    alignItems: 'center',
    aspectRatio: 16 / 9,
    borderRadius: 8,
    borderWidth: StyleSheet.hairlineWidth,
    justifyContent: 'center',
    overflow: 'hidden',
    width: 112,
  },
}).still;

export { EPISODE_STILL };
