import type { ALight } from '@ValenceMobile/components/AMoodBackground/AMoodBackground.types';

type APaletteLayerProps = {
  palette: readonly ALight[];
  isLeaving: boolean;
  isThereAlready?: boolean;
};

export type { APaletteLayerProps };
