import type { AGlyph } from '@ValenceMobile/components/Icon/Icon.types';

type AGlassCircleProps = {
  of: AGlyph;
  label: string;
  onPress: () => void;
  ink?: string;
};

export type { AGlassCircleProps };
