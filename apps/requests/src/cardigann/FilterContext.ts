import type { TemplateVariables } from '@ValenceRequests/cardigann/TemplateVariables';

type FilterContext = {
  variables: TemplateVariables;
  encoding: string;
  nowMs: number;
};

export type { FilterContext };
