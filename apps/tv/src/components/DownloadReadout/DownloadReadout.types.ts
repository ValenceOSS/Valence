import type { RequestProgress } from '@ValenceContracts/schemas/CatalogueTitle';

type DownloadReadoutProps = {
  progress: RequestProgress;
  isOnWhite?: boolean;
};

export type { DownloadReadoutProps };
