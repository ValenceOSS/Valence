import type { Format } from '@number-flow/react';

type AnimatedNumberProps = {
  value: number;
  format?: Format;
  prefix?: string;
  suffix?: string;
  className?: string;
};

export type { AnimatedNumberProps };
